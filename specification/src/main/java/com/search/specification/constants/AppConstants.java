package com.search.specification.constants;

import lombok.Getter;

public class AppConstants {
    public static enum AppOperations  {
        EQUALS, BETWEEN, LIKE
    }


    public static enum LogicalOperation {
        AND, OR
    }
}
